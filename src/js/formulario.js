document.addEventListener('DOMContentLoaded', () => {
    console.log("Módulo de formularios listo.");
    // Aquí implementaremos las reglas de validación (mayoría de edad, correos @inacap.cl, etc.)
});